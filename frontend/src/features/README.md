# Features

Each product area owns its components, query hooks, API functions, form schemas, and tests here. Shared shadcn primitives remain in `components/ui`; generic transport/configuration remains in `lib`. No product feature is implemented in Phase 0.
