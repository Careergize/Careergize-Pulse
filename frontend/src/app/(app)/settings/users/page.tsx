"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";import { apiFetch } from "@/lib/api-client";import type { Paginated,User } from "@/types/api";
export default function UsersSettings(){const [users,setUsers]=useState<User[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const load=useCallback(()=>apiFetch<Paginated<User>>("/api/v1/users/").then(x=>setUsers(x.results)).catch(e=>setError(e.message)).finally(()=>setLoading(false)),[]);useEffect(()=>{load()},[load]);
async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const form=e.currentTarget;const d=new FormData(form);try{await apiFetch("/api/v1/users/",{method:"POST",body:JSON.stringify({name:d.get("name"),email:d.get("email"),role:d.get("role"),status:"invited"})});form.reset();await load()}catch(reason){setError(reason instanceof Error?reason.message:"Could not add user")}}
return <div className="space-y-5"><form onSubmit={add} className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Add user</h2><div className="mt-4 grid gap-3 md:grid-cols-[1fr_1fr_180px_auto]"><input name="name" required placeholder="Full name" className="rounded-lg border px-3 py-2 text-sm"/><input name="email" required type="email" placeholder="Email" className="rounded-lg border px-3 py-2 text-sm"/><select name="role" className="rounded-lg border px-3 py-2 text-sm"><option value="company_admin">Company Admin</option><option value="manager">Manager</option><option value="agent">Agent</option><option value="analyst">Analyst / Viewer</option></select><button className="rounded-lg bg-primary px-4 py-2 text-sm font-medium text-white">Add</button></div><p className="mt-2 text-xs text-muted-foreground">New users are created without a password; password delivery/reset is reserved for a later email workflow.</p></form><div className="overflow-hidden rounded-xl border bg-white"><div className="border-b px-5 py-4"><h2 className="font-semibold">Users</h2></div>{error?<p className="p-5 text-sm text-red-700">{error}</p>:loading?<p className="p-5 text-sm text-muted-foreground">Loading users…</p>:users.length===0?<p className="p-8 text-center text-sm text-muted-foreground">No users found.</p>:<div className="divide-y">{users.map(u=><div key={u.id} className="grid gap-2 px-5 py-4 sm:grid-cols-[1fr_1fr_160px_100px]"><span className="font-medium">{u.name}</span><span className="text-sm text-muted-foreground">{u.email}</span><span className="text-sm capitalize">{u.role.replaceAll("_"," ")}</span><span className="text-sm capitalize text-muted-foreground">{u.status}</span></div>)}</div>}</div></div>}
